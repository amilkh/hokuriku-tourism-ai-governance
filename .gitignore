# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/
.venv_kansei/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Node (for submodule builds)
node_modules/
pnpm-lock.yaml

# JMA raw / fragmented source files (only clean merged asset is committed)
# .py scripts and READMEs inside rawdata/ are still tracked
jma/*_rawdata

# Generated outputs (reproducible — regenerate via pipeline)
output/validation_report.json
output/KANSEI_SUMMARY.txt
output/analysis_metrics.txt

# Downloaded GeoJSON cache (regenerated automatically by generate_weather_shield_map.py)
output/.cache/

# Temporary pipeline intermediates
tmp/

# Presentation/document binaries — track only the committed exec report PDFs
*.pdf
!output/pdf/*.pdf

# Learning workspace — thesis defense prep, not part of public pipeline
learning/
